# SQL Server Configuration
# Copy file này thành .env và điền thông tin của bạn

# Server name (ví dụ: localhost\SQLEXPRESS hoặc 2E6B64B1A9C3931\SQLEXPRESS)
DB_SERVER=2E6B64B1A9C3931\SQLEXPRESS
DB_DATABASE=QL_CLBvaDoiNhom

# Windows Authentication (không cần user/password)
# Để trống nếu dùng Windows Authentication
DB_USER=
DB_PASSWORD=

# Hoặc nếu dùng SQL Server Authentication, điền thông tin:
# DB_USER=sa
# DB_PASSWORD=your_password

DB_PORT=1433

# Encryption: true = Mandatory, false = Optional
DB_ENCRYPT=true
DB_TRUST_CERTIFICATE=true

# Windows Authentication: true = dùng Windows Auth, false = dùng SQL Auth
DB_USE_WINDOWS_AUTH=true

# Server Configuration
PORT=5000
NODE_ENV=development

# JWT Secret Key (thay đổi trong production)
JWT_SECRET=your-secret-key-change-in-production

